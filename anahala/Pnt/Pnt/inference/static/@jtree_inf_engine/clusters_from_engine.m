function clusters = clusters_from_engine(engine)
% clusterS_FROM_ENGINE Return the clusters stored inside the inf. engine (jtree)
% clusters = clusters_from_engine(engine)

clusters = engine.clusters;
